<h1>Usuário cadastrados</h1>
<div class="container">
    {{#each users}}
    <div class="item">
        <p>{{this.name}}</p>
        <p>{{this.occupation}}</p>
        <div class="action">
            <a href="/users/{{this.id}}">Ver detalhes</a>
        </div>
    </div>
    {{/each}}
</div>